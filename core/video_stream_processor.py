class VideoStreamProcessor():
    @staticmethod
    def process(img, configuration):
        a = img
        # Apply transformations based on config
        # if (configuration):
        # and self.configuration["overlay"]):
        # self.overlayTransformer.transform(
        #     raw, self.configuration["overlay"])
